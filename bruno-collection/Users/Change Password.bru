meta {
  name: Change Password
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/users/{{userId}}/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "oldPassword": "password123",
    "newPassword": "newpassword456"
  }
}
